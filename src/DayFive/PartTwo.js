class Computer {
  constructor(instructions, input) {
    this.pointer = 0;
    this.instructions = instructions;
    this.input = input;
  }

  computeParameterModes(opcode) {
    let stringOpcode = opcode.toString();
    let parameterModes = []
    if (stringOpcode.length > 2) {
      const opcode = 
    }
  }
}
