function getDirectionAndMovement(instruction) {
  const direction = instruction.substring(0, 1);
  const movement = instruction.substring(1);
  return { direction, movement };
}

function PartOne(wireOne, wireTwo) {
  let origin = { x: 0, y: 0 };
}

export default PartOne;
